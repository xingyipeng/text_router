# 更新记录

## 1.0.8 — 2026-09-09

- 构建阶段与运行阶段统一从自有阿里云 ACR 拉取 `base-node-22-slim` 基础镜像。
- 发布 `linux/amd64`、`linux/arm64` 双架构应用镜像，镜像标签为 `1.0.8`，验证后同步更新 `latest`。
- 延续 1.0.7 的安全与稳定性修复，无新增数据库迁移或配置要求。
- Git 标签：`release-v1.0.8`。

## 1.0.7 — 2026-09-09

### 修复与安全

- 隔离管理接口和应用资源路径，阻止普通规则覆盖接口；已有冲突规则保留在数据库中，但不再生效。
- 帮助文档使用 DOMPurify 过滤 HTML，防止事件属性、脚本和危险链接执行。
- 通配匹配改为动态规划，避免多个 `**` 组合造成主线程阻塞。
- 每日备份按日期及分钟去重，增加调度互斥，修复第二天不执行；并行手动备份使用独立临时文件。
- 正确解析 `BACKUP_ENABLED=false`，备份设置与实际执行统一使用环境预设；Compose 补齐运行设置变量。
- 自检默认只访问公网 HTTPS，校验并固定 DNS 连接地址；内网访问需配置精确主机白名单 `SELFCHECK_ALLOWED_HOSTS`。
- 捕获自检正文断流和超时，限制响应为 64 KiB，保留 BOM；批量任务逐条记录失败并正确结束，限制任务及请求并发。
- 登录增加账号、全局频率及并发限制，使用异步密码校验；密码上限 1024 字符，发会话前重新检查账号状态。

### 性能与维护

- 添加活跃规则 filename 索引和会话过期索引，避免按路径匹配时全索引扫描。
- 趋势统计改为一次遍历，明确看板仅统计本次启动后最近保留的请求，不代表全天总量。
- 更新 Hono、Vitest 及其依赖，补充安全回归测试。

### 升级说明

- 开发及测试环境要求 Node.js ≥ 22.12；Docker 使用 Node 22 镜像。
- 启动时自动补齐索引，无需手工迁移。原有规则、账号和备份格式保持兼容。
- 原来依赖内网自检的部署需配置 `SELFCHECK_ALLOWED_HOSTS`；公开文本响应不受该出站限制影响。
- Git 标签：`release-v1.0.7`。
