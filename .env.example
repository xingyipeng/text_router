# 仅在数据库为空时用于创建超级管理员，创建后这两项会被忽略。
# 不设置时的默认值是 admin / admin123（弱默认，仅用于首次启动，登录后请立即修改密码）
SUPER_ADMIN_USER=admin
SUPER_ADMIN_PASSWORD=change-this-password

PORT=3000
SESSION_TTL_HOURS=168

# 部署在 HTTPS 后面时改为 true
COOKIE_SECURE=false
